The actual tests have been created in two different directories. Smoke tests, integration tests, and system tests can be found in the keeptrack-ts/demo directory, in the files smoketests.robot, integrationtests.robot, and systemtests.robot. Additionally, unit tests have been created in the keeptrack-ts/src/projects directory, where one new unit test has been added to each of the existing files: ProjectsPage-test.tsx, ProjectList-test.tsx, ProjectForm-test.tsx, and ProjectCard-test.tsx. Furthermore, a new test file, projectDetail-test.tsx, has been created in the same directory to test the corresponding component.
